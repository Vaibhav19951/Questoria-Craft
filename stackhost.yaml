version: 1

applications:
  - name: demon_slayer_bot
    type: web
    runtime: nodejs
    build:
      commands:
        - npm install
    run:
      command: node index.js
    env:
      BOT_TOKEN: your_bot_token_here
